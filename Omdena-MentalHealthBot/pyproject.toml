[project]
name = "mental-health-chatbot"
version = "0.1.0"
description = "RAG-based mental health support chatbot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=0.3.25",
]
